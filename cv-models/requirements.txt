torch==2.14.0
torchvision==0.29.0
rasterio==1.5.1
geopandas==1.1.4
shapely==2.1.2
opencv-python==5.0.0.93
matplotlib==3.11.1
jupyter==1.1.1

# albumentations: SAR satellite datasets are small. This library performs
# ultra-fast image augmentations (flips, rotations, brightness shifts)
# to artificially multiply your dataset size and prevent the AI from overfitting.
albumentations==2.0.8

# segmentation-models-pytorch: Writing a U-Net from scratch in PyTorch takes hours and is prone
# to tensor-shape mismatch errors. This library allows to load a pre-trained, production-ready
# U-Net with a single line of code (smp.Unet(...)), saving days of ideathon debugging time.
segmentation-models-pytorch==0.5.0